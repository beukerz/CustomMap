<?xml version="1.0" encoding="utf-8"?>

<xct:Popup xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             Size="300,300"
             BackgroundColor="White"
             Opacity="1"
             x:Class="CustomMap.Views.AddLocationPopupView">
        <TableView Intent="Form">
            <TableRoot>
                <TableSection Title="Add Location">
                    <EntryCell Label="Address" Placeholder="Raadhuisstraat 97E" Text="{Binding Address}"/>
                    <SwitchCell Text="Is commercial" On="{Binding IsCommercial}" />
                </TableSection>
                <TableSection>
                    <ViewCell>
                        <Button Text="Save">
                            <Button.Behaviors>
                                <xct:EventToCommandBehavior 
                                    EventName="Clicked"
                                    Command="{Binding SaveLocationCommand}"/>
                            </Button.Behaviors>
                        </Button>
                    </ViewCell>
                </TableSection>
            </TableRoot>
        </TableView>
</xct:Popup>